<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> GR/-\FF </title>

    <link rel="stylesheet" type="text/css"  href="/css/home.css">
    <link rel="stylesheet" type="text/css"  href="/css/header.css">
</head>
<body>
    <!--Odaberi drugi graf
    <br>
    <form action="/noviodobir" method="POST" enctype="multipart/form-data" >
        <input class="gumb" type="submit" name="gumb" value="Odaberi drugi graf"> 
    </form>
    <br>
    Odaberi drugi algoritam:
    <br>
    <form action="GirvanNewmanAlgoritam" method="GET" enctype="multipart/form-data" >
        <input class="gumb" type="submit" name="gumbGirvanNewman" value="girvanNewman">
    </form>
    <br>
    <form action="betweennessCentrality" method="GET" enctype="multipart/form-data" >
        <input class="gumb" type="submit" name="gumbBetweennessCentrality" value="betweennessCentrality">
    </form>
    <br> -->


    Dijkstra

    <br>

    Opis forme

    <br>

    <form id = "parametriForm">
        <label for="cvorA"> Čvor A: </label>
        <select name="cvorA" id="cvorA">
            <% Object.keys(graf).forEach(function(key) { %>
                <option value="<%= key %>"><%= key %></option>
            <% }); %>
        </select>

        <label for="cvorB"> Čvor B: </label>
        <select name="cvorB" id="cvorB">
            <% Object.keys(graf).forEach(function(key) { %>
                <option value="<%= key %>"><%= key %></option>
            <% }); %>
        </select>

        <label for="preskakanje"> Preskakanje: </label>
        <select name="preskakanje" id="preskakanje">
            <% for(let i = 1; i <= Object.keys(graf).length; i++) { %>
                <option value="<%= i %>"><%= i %></option>
            <% }; %>
        </select>

        <button type="submit"> Pokreni </button>
    </form>

    <div id="rezultati"></div>

    <script src="/scripts/dijkstra.js"></script>

</body>
</html>